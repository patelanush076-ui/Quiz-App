import React, { useState } from 'react';\nimport PropTypes from 'prop-types';\nimport './AnswerOption.css';  // import your CSS styles here\n\nconst AnswerOption = ({ answer, onSelected, correctAnswer }) => {\n    const [isSelected, setIsSelected] = useState(false);\n    const [isCorrect, setIsCorrect] = useState(null);  // null, true, or false\n\n    const handleClick = () => {\n        if (isSelected) return; // Do nothing if already selected\n        setIsSelected(true);\n        const result = answer === correctAnswer;\n        setIsCorrect(result);\n        onSelected(answer, result);\n    };\n\n    return (\n        <button\n            className={`answer-option ${isSelected ? (isCorrect ? 'correct' : 'incorrect') : ''}`}\n            onClick={handleClick}\n            aria-pressed={isSelected}  // for accessibility\n            aria-label={`Answer option: ${answer}`}\n            tabIndex={0}\n            role="button"\n        >\n            {answer}\n        </button>\n    );\n};\n\nAnswerOption.propTypes = {\n    answer: PropTypes.string.isRequired,\n    onSelected: PropTypes.func.isRequired,\n    correctAnswer: PropTypes.string.isRequired,\n};\n\nexport default AnswerOption;